name: security-gate

on:
  workflow_dispatch:
    inputs:
      repository:
        description: 'Repository to scan (owner/repo name only)'
        required: true
        default: 'SCA-Test'
      ref:
        description: 'Branch or tag to scan'
        required: true
        type: choice
        options:
          - main
          - master
        default: 'main'
      tags:
        description: 'Tags to assign in Dependency-Track (comma-separated)'
        required: false
        default: ''

permissions:
  actions: write
  contents: write
  security-events: write

env:
  TRIVY_SEVERITY: "HIGH,CRITICAL,MEDIUM"
  TRIVY_IGNORE_UNFIXED: "false"
  DT_PROTO: http
  DT_HOST: ${{ secrets.TEST_HOSTNAME }}
  DT_PORT: ${{ secrets.TEST_PORT }}
  DT_API_KEY: ${{ secrets.TEST_API_KEY }}

concurrency:
  group: security-${{ github.event.inputs.repository }}-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  trivy_gate:
    name: Trivy Gate (fail-fast)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4
        with:
          repository: 'ngdyquan213/${{ github.event.inputs.repository }}'
          token: ${{ secrets.TEST_CI_CD }}  
          ref: '${{ github.event.inputs.ref }}'
          persist-credentials: false

      - name: Trivy FS scan (fail on HIGH/CRITICAL)
        uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          severity: ${{ env.TRIVY_SEVERITY }}
          ignore-unfixed: ${{ env.TRIVY_IGNORE_UNFIXED }}
          exit-code: '1'
          hide-progress: 'true'

  trivy_report:   
    name: Trivy Report (SARIF & SBOM)
    runs-on: ubuntu-latest   # hoặc prod-runner-set nếu đã sẵn
    needs: trivy_gate
    if: always()
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4
        with:
          repository: 'ngdyquan213/${{ github.event.inputs.repository }}'
          token: ${{ secrets.TEST_CI_CD}}
          ref: '${{ github.event.inputs.ref }}'

      - name: Generate CycloneDX SBOM
        uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'cyclonedx'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          exit-code: 0
          output: 'sbom.json'

      - name: Resolve next projectVersion (v1, v2, v3, ...)
        id: ver
        shell: bash
        env:
          PROJECT_NAME: ${{ github.event.inputs.repository }}
        run: |
          set -euo pipefail
          BASE="${DT_PROTO}://${DT_HOST}:${DT_PORT}"

          # URL-encode project name an toàn
          NAME_ENC=$(jq -rn --arg x "$PROJECT_NAME" '$x|@uri')

          echo "🔍 Finding next version for project: $PROJECT_NAME"
          # Tăng dần từ v1 đến khi gặp version chưa tồn tại
          n=1
          max=1000  # guard tránh vòng lặp vô hạn nếu có sự cố
          while (( n <= max )); do
            ver="v${n}"
            code=$(curl -s -o /dev/null -w "%{http_code}" \
              -H "X-Api-Key: ${DT_API_KEY}" \
              "${BASE}/api/v1/project/lookup?name=${NAME_ENC}&version=${ver}" || true)
            if [[ "$code" == "200" ]]; then
              # version đã tồn tại -> thử tiếp
              ((n++))
            else 
              # gặp version chưa tồn tại -> dùng nó
              echo "✅ Using new version: ${ver}"
              echo "version=${ver}" >> "$GITHUB_OUTPUT"
              break
            fi
          done        

      - name: Ensure parent project exists
        run: |
            curl -X PUT \
            -H "X-Api-Key: ${{ secrets.DT_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{"name": "${{ github.event.inputs.repository }}", "version":"1.0.0"}' \
            http://${{ secrets.TEST_HOSTNAME }}:${{ secrets.TEST_PORT }}/api/v1/project

      - name: Upload SBOM to Dependency-Track
        uses: DependencyTrack/gh-upload-sbom@v3
        with:
          protocol: 'http'
          serverhostname: ${{ secrets.TEST_HOSTNAME }}
          port: ${{ secrets.TEST_PORT }}
          apikey: ${{ secrets.TEST_API_KEY }}
          projectname: ${{ github.event.inputs.repository }}
          projectversion: ${{ steps.ver.outputs.version }}
          projecttags: ${{ github.event.inputs.tags }}
          bomfilename: ${{ github.workspace }}/sbom.json
          parentname: "Main Platform"
          parentversion: "1.0.0"
          autocreate: true