version: 2

updates:
  - package-ecosystem: "pip"      # loại gói (Python)
    directory: "/"                # thư mục chứa requirements.txt
    schedule:
      interval: "daily"           # chạy mỗi ngày
      time: "07:00"               # UTC
    open-pull-requests-limit: 10  # 👈 đặt ở đây!
    labels: ["dependencies", "security"]
    rebase-strategy: "auto"
    commit-message:
      prefix: "deps"
      include: "scope"
    groups:
      security-patches:
        applies-to: security-updates
        patterns:
          - "*"